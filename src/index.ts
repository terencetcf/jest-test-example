import { calculator } from '@utils';

const a = calculator.division(100, 2);

const b = calculator.division(a, 2);

const c = calculator.division(b, 2);

const d = calculator.division(c, 2);

const e = calculator.division(d, 2);
